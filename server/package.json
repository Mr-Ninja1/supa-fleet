{
  "name": "supa-fleet-server",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build": "npm --prefix ../client install && npm --prefix ../client run build",
    "postinstall": "npm --prefix ../client install && npm --prefix ../client run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "@supabase/supabase-js": "^2.48.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
